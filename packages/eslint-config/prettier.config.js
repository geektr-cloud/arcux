/**
 * 共享 acrux Prettier 配置。
 * 当前与仓库根 .prettierrc.json 对齐（全部使用 Prettier 默认值）。
 * 需要时在此处集中调整，所有包通过 "prettier": "@acrux/eslint-config/prettier" 复用。
 * @type {import("prettier").Config}
 */
export default {};
